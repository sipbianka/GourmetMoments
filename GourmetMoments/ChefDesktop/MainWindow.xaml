<Window x:Class="ChefDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChefDesktop"
        mc:Ignorable="d"
        Title="ChefExpenses" Height="600" Width="900" MinHeight="480" MinWidth="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFBDBDBD" BorderThickness="1" CornerRadius="2" Padding="3" Margin="0,0,0,8">
            <DataGrid x:Name="ExpensesGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      RowHeaderWidth="0"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="ChefName" Binding="{Binding ChefName}" Width="200"/>
                    <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=d}" Width="140"/>
                    <DataGridTextColumn Header="Kategoria" Binding="{Binding Kategoria}" Width="140"/>
                    <DataGridTextColumn Header="Osszeg" Binding="{Binding Osszeg}" Width="100"/>
                    <DataGridTextColumn Header="Megjegyzes" Binding="{Binding Megjegyzes}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="1" Margin="0,4,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1.6*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,8">
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="Séf neve:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="ChefNameTextBox" Width="200" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,6">
                    <TextBlock Text="Dátum:" Width="90" VerticalAlignment="Center"/>
                    <DatePicker x:Name="DatumPicker" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,6">
                    <TextBlock Text="Kategória:" Width="90" VerticalAlignment="Center"/>
                    <ComboBox x:Name="KategoriaComboBox" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,6">
                    <TextBlock Text="Összeg (EUR):" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="OsszegTextBox" Width="200"/>
                </StackPanel>
            </StackPanel>

            <Grid Grid.Row="0" Grid.Column="1">
                <Button x:Name="AddButton"
                        Content="Hozzáadás"
                        Width="150"
                        Height="70"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0,12,0,0"
                        Background="#FFD9D9D9"
                        BorderBrush="#FF9E9E9E"
                        Click="AddButton_Click"/>
            </Grid>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,6,0,0">
                <TextBlock Text="Megjegyzés:" Margin="0,2"/>
                <TextBox x:Name="MegjegyzesTextBox"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Height="120"
                         TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
